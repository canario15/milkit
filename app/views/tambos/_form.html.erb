<%= form_with(model: tambo, local: true) do |form| %>
  <% if tambo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tambo.errors.count, "error") %> prohibited this tambo from being saved:</h2>

      <ul>
      <% tambo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :phone_contact %>
    <%= form.text_field :phone_contact %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
